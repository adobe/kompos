[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --tb=short
    --disable-warnings

markers =
    integration: Integration tests that run actual kompos commands
    unit: Unit tests for individual functions
    slow: Tests that take longer to run
